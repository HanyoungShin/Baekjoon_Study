n, m = map(int, input().split())
board = []  # 입력된 보드
blackBoard = []  # 검은색으로 시작하는 보드
whiteBoard = []  # 흰색으로 시작하는 보드

for i in range(n - 7):
    for j in range(m - 7):
        board[i][j] = input()  # 보드 칸 색깔 입력
